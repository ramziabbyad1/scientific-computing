import {HierarchyFormComponent} from './hierarchy-form.component';
import {Hierarchy} from './hierarchy';
import {HierarchyService} from './hierarchy.service';
import {Level} from './level';
import {Path} from './path';
describe('Hierarchy form tests', () => {
	/*let _hierarchyService: HierarchyService = new HierarchyService();
	let MOCK_HIERARCHY: Hierarchy = _hierarchyService.getSampleHierarchy();
	let LEVELS: Level<Path>[] = MOCK_HIERARCHY.levels;
	let HIERARCHY_FORM: HierarchyFormComponent = new HierarchyFormComponent();

	it('ranges calculation', () => {
		let maxSize: number = HIERARCHY_FORM.getMaxSize(LEVELS);
		let ranges: { key: string, start: number, end: number }[][] = HIERARCHY_FORM.getRanges(LEVELS, maxSize);
		console.log(ranges);
		expect(true).toEqual(true);
	});*/
});